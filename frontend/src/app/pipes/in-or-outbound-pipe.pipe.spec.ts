import { InOrOutboundPipePipe } from './in-or-outbound-pipe.pipe';

describe('InOrOutboundPipePipe', () => {
  it('create an instance', () => {
    const pipe = new InOrOutboundPipePipe();
    expect(pipe).toBeTruthy();
  });
});
